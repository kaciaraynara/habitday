package com.example.habitday.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta Unificada HabitDay (Baseada no Logo Oficial - Azul, Amarelo, Branco)
val BrandBlue = Color(0xFF007AFF)      // Azul Vibrante (Identidade principal)
val BrandYellow = Color(0xFFFFC107)    // Amarelo (Acento e Sol do logo)
val BackgroundWhite = Color(0xFFFFFFFF) // Fundo Limpo e Principal

// Fundos suaves personalizáveis (conforme pedido)
val BgGray = Color(0xFFF5F7F9)
val BgCreme = Color(0xFFFFFDF0)
val BgBlue = Color(0xFFF0F7FF)
val BgGreen = Color(0xFFF2FAF2)

val BrandBlueLight = Color(0xFFE3F2FD) // Azul fundo suave para cards
val TextPrimary = Color(0xFF0A1F33)    // Azul quase preto para leitura
val TextSecondary = Color(0xFF526E82)  // Cinza azulado
val SuccessGreen = Color(0xFF34C759)   // Verde iOS/Profissional
val ErrorRed = Color(0xFFFF3B30)       // Vermelho Vibrante
val BorderBlue = Color(0xFFB3D7FF)     // Borda sutil azul
val SurfaceLight = Color(0xFFF2F7FF)   // Superfície sutil azulada
